fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
httpx>=0.27.0
python-dotenv>=1.0.1
clickhouse-connect>=0.8.0

# Google Cloud / Gemini. Vertex AI is reached via Application Default
# Credentials (gcloud auth application-default login) - no API key required.
google-genai>=1.0.0
google-adk>=1.0.0
# Vertex access is provided through google-genai. The legacy
# google-cloud-aiplatform package is not required by the runtime.

# OpenTelemetry -> Grafana Tempo. Pinned as a set: google-adk constrains the
# SDK, and a mismatched exporter breaks trace export at import time.
opentelemetry-api==1.42.1
opentelemetry-sdk==1.42.1
opentelemetry-exporter-otlp-proto-http==1.42.1

pillow>=10.4.0
gTTS>=2.5.3
orjson>=3.10.0
sse-starlette>=2.1.0
